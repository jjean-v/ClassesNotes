== Cryptographie

# Introduction to Cryptographie

The study of techniques to allow secure communciation and datea storafe in presence of attackers

- Confidentiality: data can be accessed only by chosen entities
- Integrity/freshness: detect/prevent tampering or replays
- Authenticity: data and their origin are certified
- Non-repudiation: data creator cannot repudiate created data
- Advanced features: proofs of knowledge/computation


## 1553 - Bellaso

- Write a text on how to code a text
- **Separates the encryption method from the key**

## 1883 - Kerchoff's six principles for a good cipher

- It must be practically, if not mathematically unbreakable
- **It should be possible to make it public, even to the ennemy**
- The key must be communicable without written notes and changeable whenever the correpondants want
- It must be applicable to telegraphic communication
- It must be portable, and should be operable by a single person
- Finally, given the operating environment, it should be easy to use, it shouldn't impose excessive mental load, nor require a large set of rules to be known


## Modern History

* Mechanical compution changes cryptography
** First rotor machine in 1917 by Ed Hebern
** Design "popularized" in WWII by German Enigma
* Cryptanalysist at Bechley park (Turing among them) credited for a decisive effort in winnning the war by Eisenhower

## 1950 - Syannon - Nash

- Proves that a mathematically unbreakable cipher exists

### 1955 - Nash

* Argues that computationally secure ciphers are ok
** Consider a cipher with a finite, lambda bit long, key
**Takes too long time for the attackers
** **Called computational security**


## Outlines of the topics

- Definition of ciphers as components with functionalities
- How to obtain confidentiality, integrity, data,/origin authentication
- An overview of protocols (combination of ciphers)
- Goal: be able to **use** crytographhic components properly


### Before we start

#### Randomness

Randomness has nothing to do with value but how you generate them

### Definitions

- Paintexte space **P**: set f
- Cypher text **C**
- Text **K**

#### Functions 

- Encryption function
- Decryption function

Insert image


#### Shonnon Theorem 1949

- The key should be fresh for any new message
- For any plaintext you get a unique cipher text
- No 2 plaintext give you the same cipher text
- It's **Bijective**

### Problem key management

- Storing key material and changing keys is a nightmare
- Perfect cipher broken in practice due to key theft/reuse
- Generating random keys was also an issue (and caused breaks)

## Computationnaly secure cryptography

* Build  cipher so that a succesful attack is also able to solve a hard computational problem efficiently
** Solve a generic nonlinear Boolean simultaneous equation set
** Factor large intefers/ find discrete logarithms
** Decode a random code/ find shortest lattice vector

* Open question: prove an exponential lower bound for the time taken to salve a hard problem, which has efficiently verifiable solution
** it would shift form a computation securitu **assumption** to a theorem
** ... and prove **P!=NP** as a **corollary**


### Outline of te method

- Define the ideal attacker behaviour
- Assume a given computational problem is hard
- Prove that any non ideal attacker solves the hard problem

### Motivation and assumption

* We want to use a finite-length they and a Vernam cipher
** We somehow need to "expand" the key
* We assume that the attacker can only perform poly(lambda) computation

### Cryptographically Safe Pseudorandom Number Generators

#### Definition

    A CSPRNG is a deterministic function prng: {0, 1}^λ^ → {0, 1}
    ^λ+l^ whose output cannot be distinguished from an uniform random sampling of {0, 1}^λ+l^ in O(poly(λ)). l is the CSPRNG stretch.

### CSPRNGs in practice

* In practice, we have only candidate CSPRNGs
** We have no proof that a function prng exists
** Proving that a CSPRNG exists implies directly P =! NP


#### How to build a CSPRNG

4 steps

* **Phase 1**: Coins {0,1}
* **Phase 2**: you have a input, and a boolean function, you use it to have different Coins
* **Phase 3**: Same but, with a deterministic function 

The goal is, given the inputs, you shouldn't be able to know if it comes from phase 1, 2 or phase 3.

Exist something in cryptography that is a phase 3 but slow, 1 Kb/s. 

We simplify the problem to find a solution

* It is a bijective function


## Integrity

* MAC is constitued by a pair of Functions
** COMPUTE_TG returns the tag for the input Storing
** VERIFY_TAG returns true or false 
* Used to preserve the integrity of the cookie


## Cryptographic Hash


* A cryptographic hash is a function H : {0, 1}^∗^ → {0, 1}^l^ for which the following problems are computationally hard
.. given d = H(s) find s (1st preimage)
.. given s, d = H(s) find r 6= s with H(r) = d (2nd preimage)
.. find r,s;r 6= s, with H(s) = H(r) (collision)
* Ideal behaviour of a concrete cryptographic hash:
. finding 1st preimage takes O(2^d^) hash computations guessing s
. finding 2nd preimage takes O(2^d^) hash comp.s guessing r
. finding a collision takes ≈ O(2^d/2^) hash computations
* The output bitstring of a hash is known as a digest

Hashes are 3 to 2 times slower thant block cypher

* SHA-2 still used but designed look alike SHA-1 which was broken
* No reason to keep SHA-2
* We know the specification and implementation but not the design criteria
** We don't know if there is a faster method to decode than using brute force

### Use for hash function

* Store/compare hashes instead of values
** Much faster to hash to files and compare then than to compare the files

MACs

* Building MACs: generate tag hashing together the message and a secret string, verify tag recomputing the same hash
* A field-proven way of combining message and secret is HMAC
** Standardized (RFC 2104, NIST FIPS 198)
** Uses a generic hash function as a plug-in, combination denoted as HMAC-hash name
** Just need the first preimage, not the collision so SHA1 is secure for MAC
***  HMAC-SHA1 (!), HMAC-SHA2 and HMAC-SHA3 are ok


### Key Encapsulation

A public channel between Alice and Bob is available

* For the moment, the attacker model is “eavesdrop only” Sharing a secret without agreement
* Alice: generates a keypair (kpri, kpub), sends to Bob
*  Bob: gets s ← {0, 1}^λ^, encrypts it with k~pub~, sends ctx to Alice
* Alice: decrypts ctx with k~pri~, recovers s
* Note: Bob alone decides the value of the shared secret s
** Repeat the procedure with swapped role

### Best of Both Worlds

encryption schemes

* Asymmetric schemes to provide key transport/agreement (cypher than agreement on the cypher)
* Symmetric schemes to encrypt the bulk of the data
* All modern secure transport protocols built around this idea

Problem

You have to be sure that the ones sending you his public key is the good person

* Big risk of Man in the Midlle

=> Needs of **authentication**

## Authenticating data

Motivations

*To build a secure hybrid encryption scheme we need to be sure that the public key the sender uses is the one of the recipient
* We’d like to be able to verify the authenticity of a piece of data without a pre-shared secret 

Digital signatures

* Provide strong evidence that data is bound to a specific user
* No shared secret is needed to check (validate) the signature
* Proper signatures cannot be repudiated by the user
* They are asymmetric cryptographic algorithms
** formally proven that you cannot get non repudiation otherwise
* No Repudiation


### Widespread Signature Schemes

Rivest, Shamir, Adleman (RSA), 1977

* Unique case: the same hard-to-invert function to build an asymmetric encryption scheme and a signature (different message processing!)
* Signing definitely slower than verification (≈ 300×)
* Standardized in NIST DSS (FIPS-184-4)

Digital Signature Standard (DSA)

* Derived from tweaking signature schemes by Schnorr and Elgamal
** By The USA but they weakened it, minefield for the implementer
** SOny case, draw a ramdom number
*** => With two hash they can guess the key
* Also standardized in NIST DSS (FIPS-184-4)
* Signature and verification take roughly the same time

### Digital certificates

* They bind a public key to a given identity, which is:
** for humans: an ASCII string
** for machines: either the CNAME or IP address
* They specify the intended use for the public key contained
** Avoids ambiguities when a key format is ok for both an encryption and a signature algorithm
* They contain a time interval in which they are valid
* Most widely deployed format is described in ITU X.509


### Certification Authorities

Who signs the certificates

* The certificate signer is a trusted third party, the CA
* The CA public key is authenticated... with another certificate
* Up to a self-signed certificate which has to be trusted a priori
** The browser has some certificate he know he can trust


image::cs-ca-scheme.png[]

### Putting it all together

image::cs-secure-communication.png[]

### Direction in modern cryptography

Issues to solve, features to realize

* What if we have a quantum computer?
** Some computationally hard problems are no longer hard
*** I gave you g^a^ you give me a .-.
** Move away from cryptosystems based on factoring/dlog
** Alternatives available and being standardized (2022-04)
* What if we want to compute on encrypted data?
** Yes, but it’s moderately-to-horribly inefficient
* What if the attacker has physical access to the device computing the cipher (or some way of remotely measure it)
** Take into account side channel information in the attacker mode
*** Infrared light emating from the transistor of your chip
* We still can do encryption and decryption smthng
** Just increase the number of bits

* For digital signature you can just pick two of the three
** Fast, Short key, short ...